<h1> <%=@destination.name%> | <%=@destination.country%></h1>

<h2> Total Posts: <%=@destination.posts.count%></h2>

<%if(@destination.posts.count>0)%>
    <h2>Average Age: <%=@destination.average_age%> </h2>
    <h2> Featured Post: <%= link_to @destination.featured_post.title , @destination.featured_post %></h2>
    <h2> Recent Posts:</h2>
    <ul>
        <%@destination.recent_posts.each do |post|%>
            <li><%=post.created_at.strftime("%D") %> | <%= link_to post.title, post %></li>
        <% end %>
        
    </ul>
<% end %>

